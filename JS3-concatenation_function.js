// Create a function that takes in two arguments
// and returns a concatenated string either as a console.log or alert.

// define a function that takes in two strings and concatenate them
function concatenateStrings(str1, str2) {
  return str1 + " " + str2;
}

// console.log the concatenated stirng
console.log(concatenateStrings("Good", "morning/•v•/"));